@page
@model KoiFarmShop.WebApplication.Pages.Discounthtml.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Discount[0].Koi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discount[0].Pro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Discount) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Koi.KoiId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pro.ProId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DiscountId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DiscountId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DiscountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
